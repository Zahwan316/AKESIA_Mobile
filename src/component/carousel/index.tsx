import { JSX } from "react";
import { ImageSourcePropType, Text, View } from "react-native";


type props = {
  data: string[],
}

const CarouselComponent = ({ data }: props): JSX.Element => {
  return(
    <View>
      
    </View>
  );
};

export default CarouselComponent;
