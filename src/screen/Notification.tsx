import { JSX } from 'react';
import NotificationSection from '../section/home/notification';

const NotificationScreen = (): JSX.Element => {
  return <NotificationSection />;
};

export default NotificationScreen;
