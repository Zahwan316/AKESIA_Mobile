import { JSX } from 'react';
import PemeriksaanSection from '../../section/pemeriksaan';

const PemeriksaanScreen = (): JSX.Element => {
  return(
    <PemeriksaanSection />
  );
};

export default PemeriksaanScreen;
