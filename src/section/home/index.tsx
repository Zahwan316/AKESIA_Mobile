import { JSX } from "react";
import { Text } from "react-native";
import { SafeAreaView, View } from "react-native";

const HomeSection = (): JSX.Element => {
  return(
    <SafeAreaView>
      <View>
        <Text>Hi, Lorem ipsum dolor</Text>
      </View>
    </SafeAreaView>
  );
};

export default HomeSection;
